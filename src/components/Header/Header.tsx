import classes from './Header.module.css';

const Header: React.FC = () => {
  return (
    <header className={classes['App-header']}>
      <span>George Fx Test</span>
    </header>
  );
};

export default Header;
